// Material UI è la libreria più importante e specifica per react (barra di ricerca MUI.com), è complementare a boostraps perché questo nasce per gli stili, mentre il secondo da dei componenti già fatti da copiare e incollare, già animati, da copiare e incollare PER INSTALLARE: (Cerca install su google)

//Material ui icons da cercare su google per installare le icone (la versione aggiornata del codice ha sia icon che il material base)